<div class="container">
  <h1>Results</h1>
  <% if @included %>
    <% if @english %>
      <p><strong>Congratulation! </strong><%= params[:word].upcase%> is a valid English word.</p>
    <% else %>
      <p>Sorry but <strong><%= params[:word].upcase%></strong> does not seem to be a valid English word...</p>
    <% end %>
  <% else %>
    <p>Sorry but <strong><%= params[:word].upcase%></strong> can't be built out of <%=params[:grid_token] %></p>
  <% end %>
  <h3>Score: <%= @score.round(3) %></h3>
  <%= link_to "Play Again", "/new" %>
</div>
